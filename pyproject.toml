[project]
name = "water_m"
version = "0.1.0"
description = "LoRaWAN Water Meter C2 Protocol Backend"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1",
    "gunicorn>=23.0",
    "whitenoise>=6.7",
    "python-dotenv>=1.0",
    "requests>=2.31",
]

[dependency-groups]
dev = []
